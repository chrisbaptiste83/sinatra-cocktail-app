<!DOCTYPE html>
<html lang="en" data-theme="mytheme">
  <head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <meta name="description" content="Chris's Cocktail Collection - Discover and create amazing cocktail recipes">
    <meta name="author" content="">

    <title>Chris's Cocktail Collection</title>

    <link rel="stylesheet" href="/stylesheets/application.css">

    <style>
      body {
        background: linear-gradient(
          rgba(20,20,20, .5),
          rgba(20,20,20, .5)),
          url(/images/Bar3.jpg);
        background-attachment: fixed;
        background-size: cover;
      }

      img {
        border-radius: 0.5rem;
      }
    </style>
  </head>

  <body class="min-h-screen">
    <div class="navbar bg-base-300 shadow-lg fixed top-0 w-full z-50">
      <div class="navbar-start">
        <a href="/cocktail_recipes" class="btn btn-ghost text-xl normal-case">
          <span class="text-primary font-bold">Chris's Cocktail Collection</span>
        </a>
      </div>

      <div class="navbar-center hidden lg:flex">
        <ul class="menu menu-horizontal px-1">
          <% if logged_in? %>
            <li><a href="/cocktail_recipes">Cocktails</a></li>
            <li><a href="/users" class="hidden">Users</a></li>
            <li><a href="/cocktail_recipes/new" class="btn btn-primary btn-sm">Create Cocktail</a></li>
          <% end %>
        </ul>
      </div>

      <div class="navbar-end">
        <% if logged_in? %>
          <div class="dropdown dropdown-end">
            <label tabindex="0" class="btn btn-ghost btn-circle avatar placeholder">
              <div class="bg-neutral text-neutral-content rounded-full w-10">
                <span class="text-xl"><%= current_user.username[0].upcase %></span>
              </div>
            </label>
            <ul tabindex="0" class="menu menu-sm dropdown-content mt-3 z-[1] p-2 shadow bg-base-200 rounded-box w-52">
              <li><a href="/users/<%= current_user.username %>">Profile</a></li>
              <li>
                <form method="post" action="/logout">
                  <input type="hidden" name="_method" value="delete">
                  <button type="submit" class="text-error w-full text-left">Logout</button>
                </form>
              </li>
            </ul>
          </div>
        <% else %>
          <div class="flex gap-2">
            <a href="/login" class="btn btn-ghost btn-sm">Login</a>
            <a href="/signup" class="btn btn-primary btn-sm">Sign Up</a>
          </div>
        <% end %>
      </div>
    </div>

    <div class="pt-20 pb-8">
      <div class="container mx-auto px-4 py-8">
        <% if flash.has?(:message) %>
          <div class="alert alert-info shadow-lg mb-4">
            <svg xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" class="stroke-current shrink-0 w-6 h-6"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13 16h-1v-4h-1m1-4h.01M21 12a9 9 0 11-18 0 9 9 0 0118 0z"></path></svg>
            <span><%= flash[:message] %></span>
          </div>
        <% end %>

        <%= yield %>
      </div>
    </div>

    <footer class="footer footer-center p-10 bg-base-300 text-base-content rounded mt-8">
      <div>
        <p class="font-bold text-lg">Chris's Cocktail Collection</p>
        <p>Discover and create amazing cocktail recipes</p>
        <p>Copyright &copy; <%= Time.now.year %> - All rights reserved</p>
      </div>
    </footer>
  </body>
</html>